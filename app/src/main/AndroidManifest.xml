<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.northuniversity.schoolteam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modules.Team.WebActivity"></activity>
        <activity android:name=".modules.Recommend_fragment.Inside.ConcreteRecommendContest" />
        <activity android:name=".modules.Message.ChatActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".utils.TestDataActivity" />
        <activity android:name=".modules.Person.ChangeMessageActivity" />
        <activity android:name=".modules.Person.PersonMessageActivity" />
        <activity android:name=".modules.Team.Inside_activity.ReleaseActivity" />
        <activity android:name=".modules.Team.Inside_activity.ConcreteActivity" />
        <activity android:name=".modules.Team.Inside_activity.ClassificationActivity" />
        <activity android:name=".modules.Message.TeamList" />
        <activity android:name=".modules.Message.MessageActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".MyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>